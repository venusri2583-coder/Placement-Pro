<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PlacementPro | Elite Portal</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&family=Space+Grotesk:wght@300;500;700&family=Outfit:wght@400;800&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>

    <style>
        /* --- GLOBAL MODERN SETTINGS --- */
        /* Prioritizing the new stylish font while keeping Poppins as fallback */
        body { 
            font-family: 'Space Grotesk', 'Poppins', sans-serif; 
            background: #f1f5f9; 
            color: #1e293b; 
            overflow-x: hidden;
            transition: background 0.5s ease;
        }

        /* --- YOUR ORIGINAL FEATURES (PRESERVED 100%) --- */
        .hero-section { 
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); 
            color: white; 
            padding: 100px 0; 
            border-radius: 0 0 50px 50px; 
        }
        
        .glass-card { 
            background: white; 
            border-radius: 25px; 
            border: 1px solid #e2e8f0; 
            transition: 0.3s ease; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.05); 
        }
        
        .glass-card:hover { 
            transform: translateY(-10px); 
            box-shadow: 0 20px 40px rgba(99, 102, 241, 0.1); 
        }
        
        .btn-wow { 
            background: linear-gradient(90deg, #6366f1, #a855f7); 
            color: white; 
            border: none; 
            padding: 12px 35px; 
            border-radius: 50px; 
            font-weight: 700; 
        }
        
        .quote-box { 
            background: rgba(255,255,255,0.1); 
            border-left: 4px solid #fbbf24; 
            padding: 15px; 
            border-radius: 10px; 
            margin-top: 20px; 
        }

        /* --- NEW STYLISH HEADING CLASS --- */
        .stylish-heading { 
            font-family: 'Outfit', sans-serif; 
            font-weight: 800; 
            letter-spacing: -1.5px; 
        }

        /* Smoothing the Wow animations */
        .animate__animated {
            --animate-duration: 0.8s;
        }

        /* Modern focus state for your existing inputs */
        .form-control:focus {
            box-shadow: 0 0 0 0.25rem rgba(59, 130, 246, 0.25);
            border-color: #3b82f6;
        }
    </style>
</head>
<body>