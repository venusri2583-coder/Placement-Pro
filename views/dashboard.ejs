<%- include('partials/header') %>

<div style="background-color: #1a1a1a; padding-bottom: 20px; border-bottom: 3px solid #3182ce; position: relative; z-index: 1;">
    <nav class="container d-flex justify-content-between align-items-center py-2">
        <div class="text-white fw-bold h6 m-0">🚀 PLACEMENT PRO</div>
        <div class="d-flex align-items-center text-white">
            <% if (user) { %>
                <span class="me-2 opacity-75" style="font-size: 0.75rem;">Hi, <%= user.username %>! ✨</span>
                <a href="/logout" class="btn btn-outline-light rounded-pill px-2 py-0 fw-bold shadow-none" style="font-size: 0.65rem;">Logout</a>
            <% } %>
        </div>
    </nav>

    <div class="container text-center mt-1 text-white">
        <h5 class="fw-bold mb-1">Dream Big. <span style="color: #ecc94b;">Work Hard.</span> ✨</h5>
        
        <div class="mx-auto p-1" style="max-width: 400px; background: rgba(255,255,255,0.05); border-left: 2px solid #ecc94b; border-radius: 4px;">
            <p class="fst-italic mb-0" style="font-size: 0.8rem; line-height: 1.2;">"The best way to predict your future is to create it."</p>
            <small class="opacity-50" style="font-size: 0.65rem;">— Abraham Lincoln</small>
        </div>
    </div>
</div>

<div class="container" style="margin-top: 30px; padding-bottom: 40px;">
    <div class="row g-2 justify-content-center">
        
        <% 
        const items = [
            { t: 'Aptitude', i: '3401591', l: '/aptitude-topics', c: 'primary', e: '💡' },
            { t: 'Reasoning', i: '8637272', l: '/reasoning-topics', c: 'primary', e: '🧠' },
            { t: 'English', i: '3898150', l: '/english-topics', c: 'info', e: '🗣️' },
            { t: 'Technical', i: '1157109', l: '/coding', c: 'success', e: '💻' },
            { t: 'Rankings', i: '3112946', l: '/leaderboard', c: 'warning', e: '🏆' },
            { t: 'Interview', i: '9983339', l: '/interview-prep', c: 'info', e: '🤝' },
            { t: 'Resume', i: '2875437', l: '/resume-upload', c: 'danger', e: '📄' },
            { t: 'Mock Test', i: '9406364', l: '/mock-test', c: 'dark', e: '🎯' }
        ];
        %>

        <% items.forEach(item => { %>
        <div class="col-6 col-md-4 col-lg-3">
            <div class="card h-100 shadow-sm border-0 text-center p-2 hover-card">
                <img src="https://cdn-icons-png.flaticon.com/512/<%= item.i.substring(0,4) %>/<%= item.i %>.png" width="30" class="mx-auto mb-1" alt="<%= item.t %>">
                <h6 class="fw-bold mb-0" style="font-size: 0.8rem;"><%= item.t %></h6>
                <a href="<%= item.l %>" class="btn btn-sm btn-outline-<%= item.c %> rounded-pill py-0 mt-2 w-100" style="font-size: 0.65rem;">Go</a>
            </div>
        </div>
        <% }) %>

    </div>
</div>

<style>
    .hover-card { border-radius: 12px; transition: 0.2s; background: white; border: 1px solid #eee !important; }
    .hover-card:hover { transform: translateY(-3px); box-shadow: 0 4px 10px rgba(0,0,0,0.05) !important; }
    body { background-color: #f8f9fa; font-family: 'Poppins', sans-serif; }
</style>